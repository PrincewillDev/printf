#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int _putchar(char c);
int _puts(char *s);
int _sprintf(char *str, const char *format, ...);

int _printf(const char *format, ...);

#endif /* MAIN_H */
